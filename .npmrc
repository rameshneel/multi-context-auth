# NPM Registry Authentication
# Replace YOUR_TOKEN_HERE with your actual granular access token
# Format: //registry.npmjs.org/:_authToken=npm_xxxxxxxxxxxxx
# 
# OR use browser authentication (recommended):
# Run: npm publish --access public
# Then press ENTER when prompted to open browser

# Uncomment and add your token below:
# //registry.npmjs.org/:_authToken=YOUR_TOKEN_HERE
